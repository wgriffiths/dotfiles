[user]
	name = William Griffiths
	email = william@ycymro.com
[push]
	default = simple
[core]
	excludesfile = /Users/will/.gitignore
	default = tracking
	autocrlf = input
[color]
	branch = auto
	diff = auto
	interactive = auto
	status = auto
[alias]
	so = remote show origin
	po = remote prune origin
[diff]
	tool = meld
[filter "lfs"]
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
	clean = git-lfs clean -- %f
