function s {
  if [[ $# > 0 ]]; then
    svn $@
  else
    svn status
  fi
}

function g {
  if [[ $# > 0 ]]; then
	  print -z boom;
    git $@
  else
    git status
  fi
}

compdef g=git

source $HOME/.dotfiles/zsh/_functions/automotive